#include "pipe_line.h"
#include "tunnel.h"

typedef struct pipetunnel_line_state_s
{

    bool up_piped;
    bool dw_piped;

} pipetunnel_line_state_t;

tunnel_t *pipetunnelCreate(tunnel_t *t)
{
    tunnel_t *pt = newTunnel(tunnelGetNode(tunnel_t *self), tunnelget, uint16_t cstate_size)
}